# C-SHARP-Stack-Operations
Create generic class name mystack, which only contains two methods push and pop. It must provide or simulate functionality (push and pop) of .net in built generic class stack.
